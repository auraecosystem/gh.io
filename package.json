{
  "name": "web4-os",
  "version": "0.1.0",
  "private": true,
  "description": "Web4 OS runtime experiments and static assets.",
  "type": "commonjs",
  "scripts": {
    "start": "node src/Main.cjs",
    "check": "node --check src/Main.cjs && node --check runtime/Controller.cjs && node --check Kernel/Ai.cjs && node --check core/Eventbus.cjs && node --check ui/statushud.cjs",
    "test": "node --test",
    "build": "node scripts/build-static-site.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
